# Leave unset (or empty) to use the built-in mock transport.
# No backend is required to run the app locally.
EXPO_PUBLIC_API_URL=

# Set to your API server base URL to use the real HTTP transport.
# EXPO_PUBLIC_API_URL=https://api.your-backend.com
